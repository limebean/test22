<% current_user.children.each do |child|%>
	<div class="col-md-6 left_wall_reduce">
		<div class="frame_of_child">
			<div class="full_name">
				<span>Born</span><%= child.full_name%></div>
			<div class="date_of_birth">
				<%= child.date_of_birth%>
			</div>
		</div>
	</div>
<% end %>